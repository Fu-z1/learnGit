inherit: [buildenv, packing-module, werror, capigen]

metaEnvironment:
   PKG_RESPONSIBLE: "Navigation-System"

depends:
  - communication::ext-commonapi-runtime-dev
  - communication::tsd-communication-binding-dev
  - use: []
    depends:
      - communication::ext-commonapi-runtime-target
      - communication::tsd-communication-binding-target

checkoutSCM:
   scm: git
   url: git@${DEFAULT_PCC_GIT_SERVER}:mib3-navigation/tsd.nav.testinterface.api.git
   dir: tsd.nav.testinterface.api
   tag: mqb_sop1_eu-v17.0.1 #navi-interface (interface to externel contractors)
   # see link for change prozess: https://wiki-automotive.server.technisat-digital/display/MIB3SERIE/Schnittstellen+Navigation

checkoutDeterministic: true
checkoutScript: CapiGenProcessFIDLFiles tsd.nav.testinterface.api

buildScript: |
   buildenvBuildAll

# No warning-as-error for generated code
packageVars: [LIBDIR]
multiPackage:
   dev:
      packageScript: |
         buildenvPackageDev install
      provideDeps: ['*-dev']
   target: # Shared-lib (FIDL)
      packageScript: |
         buildenvPackageTarget
         d=$(mktemp -d)
         rsync -a --delete --exclude ".debug" ./ $d
         buildSwupModule -n "nav-testinterface-api" \
            -p "emmc.navigation" \
            -o "navigation:tsd-nav-testinterface-api " -f $d -- \
            -U "Partition=rootfs_ro"
         rm -rf $d
      provideDeps: ['*-target']
