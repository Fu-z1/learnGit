inherit: [buildenv]

metaEnvironment:
   PKG_LICENSE: "proprietary"
   PKG_NAME: "NavTools Utilities"
   PKG_RESPONSIBLE: "Navigation-Zielfuehrung"
#depends:

checkoutSCM:
   scm: git
   url: git@${DEFAULT_PCC_GIT_SERVER}:mib3-navigation/ext.ntut.git
   dir: ext.ntut
   tag: 131.0.0

buildScript: |
   buildenvBuildAll

multiPackage:
   dev:
      packageScript: |
         buildenvPackageDev install
         #provideDeps:
