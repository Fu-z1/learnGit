inherit: [buildenv, packing-module, werror, capigen]

depends:
  - communication::ext-commonapi-runtime-dev
  - communication::tsd-communication-binding-dev
  - system::tsd-common-dev

  - use: []
    depends:
      - communication::ext-commonapi-runtime-target
      - communication::tsd-communication-binding-target
      - system::tsd-common-target

checkoutSCM:
   scm: git
   url: git@${DEFAULT_PCC_GIT_SERVER}:mib3-system/tsd.logging.mib3.testinterface.api.git
   dir: tsd.logging.mib3.testinterface.api
   #tag: mqb_sop1_eu-1.7.0
   #branch: master_mqb_sop1_eu
   branch: main/svw_cns3.0_1480_dev

checkoutScript: CapiGenProcessFIDLFiles tsd.logging.mib3.testinterface.api
checkoutDeterministic: true

multiPackage:
   dev:
      buildScript: |
         buildenvBuildAll
      packageScript: |
         buildenvPackageDev install
      provideDeps:
        - "*-dev"

   target:
      buildScript: |
         buildenvBuildAll
      packageScript: |
         buildenvPackageTarget
         d=$(mktemp -d)
         rsync -a --delete --exclude ".debug" ./ $d
         buildSwupModule -n "logging-mib3-testinterface-api" \
            -p "emmc.system" \
            -o "system:tsd-logging-mib3-testinterface-api " -f $d -- \
            -U "Partition=rootfs_ro"
         rm -rf $d
      provideDeps:
        - "*-target"

   doc:
      buildTools: [doxygen, dot]
      buildScript: buildenvBuildDocAll
      packageScript: buildenvPackageDoc
