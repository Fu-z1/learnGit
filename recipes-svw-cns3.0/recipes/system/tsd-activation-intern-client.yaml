inherit: [buildenv, packing-module]

depends:
  - system::tsd-common-dev
  - system::tsd-activation-intern-api-dev
  - use: []
    depends:
      - system::tsd-common-target
      - system::tsd-activation-intern-api-target

checkoutSCM:
   scm: git
   url: git@${DEFAULT_PCC_GIT_SERVER}:mib3-security/tsd.activation.intern.client.git
   dir: tsd.activation.intern.client
   tag: 1.1.0
   branch: master_mqb_sop1_eu

multiPackage:
   dev:
      buildScript: |
         buildenvBuildAll
      packageScript: |
         buildenvPackageDev install
      provideDeps:
        - "*-dev"

   target:
      buildScript: |
         buildenvBuildAll
      packageScript: |
         buildenvPackageDev install
         d=$(mktemp -d)
         rsync -a --delete --exclude ".debug" ./ $d
         buildSwupModule -n "activation-intern-client" \
            -p "emmc.system" \
            -o "system:tsd-activation-intern-client " -f $d -- \
            -U "Partition=rootfs_ro"
         rm -rf $d
      provideDeps:
        - "*-target"

   doc:
      buildTools: [doxygen, dot]
      buildScript: buildenvBuildDocAll
      packageScript: buildenvPackageDoc
