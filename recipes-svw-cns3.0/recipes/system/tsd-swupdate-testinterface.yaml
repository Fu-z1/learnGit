inherit: [buildenv, packing-module, werror]

metaEnvironment:
   PKG_RESPONSIBLE: "System-Basisfunktion"

depends:
  - if: "$(eq,${TSD_BUILDENV_ENABLE_TEST_TARGETS:-0},1)"
    depends:
      - system::tsd-swupdate-ta-stub
  - system::tsd-common-dev
  - system::tsd-swupdate-client-dev
  - system::tsd-swupdate-viewcontroller-dev
  - system::tsd-swupdate-testinterface-api-dev

  - use: []
    depends:
      - system::tsd-common-target
      - system::tsd-swupdate-client-target
      - system::tsd-swupdate-viewcontroller-target
      - system::tsd-swupdate-testinterface-api-target

checkoutSCM:
   scm: git
   url: git@${DEFAULT_PCC_GIT_SERVER}:mib3-system/tsd.swupdate.testinterface.git
   dir: tsd.swupdate.testinterface
   tag: 2.5.0
   branch: master_mqb_sop1_eu

multiPackage:
   dev:
      buildScript: |
         buildenvBuildAll
      packageScript: |
         buildenvPackageDev install
      provideDeps:
        - "*-dev"

   target:
      buildScript: |
         buildenvBuildAll
      packageScript: |
         buildenvPackageTarget install
         d=$(mktemp -d)
         rsync -a --delete --exclude ".debug" ./ $d
         buildSwupModule -n "swupdate-testinterface" \
            -p "emmc.system" \
            -o "system:tsd-swupdate-testinterface " -f $d -- \
            -U "Partition=rootfs_ro"
         rm -rf $d
      provideDeps: ['*-target']

   unittests:
      buildScript: buildenvTestAll
      packageScript: buildenvPackageTests
