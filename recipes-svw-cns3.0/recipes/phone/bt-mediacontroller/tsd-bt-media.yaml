inherit: [buildenv, packing-module, werror]

metaEnvironment:
   PKG_RESPONSIBLE: "Connect-Phone"

depends:
 - phone::bt-middleware::tsd-blueware-api-dev
 - system::tsd-common-dev
 - libs::tsd-common-stm-dev
 - system::tsd-threadmessages-dev
 - phone::tsd-phone-common-dev

checkoutSCM:
 - scm: git
   url: git@${DEFAULT_PCC_GIT_SERVER}:tip-ceconnect/tsd.bt.media.git
   dir: tsd.bt.media
   tag: mqb_sop1_eu-1.145.2
multiPackage:
   dev:
      buildScript: |
         buildenvBuildAll

      packageScript: buildenvPackageDev
      provideDeps:
       - phone::bt-middleware::tsd-blueware-api-dev
       - system::tsd-common-dev
       - libs::tsd-common-stm-dev
       - system::tsd-threadmessages-dev
       - phone::tsd-phone-common-dev

   target:
      buildScript: buildenvBuildAll
      packageScript: |
         buildenvPackageTarget
         d=$(mktemp -d)
         rsync -a --delete --exclude ".debug" ./ $d
         buildSwupModule -n "bt-media" \
            -p "emmc.phone.bt-mediacontroller" \
            -o "phone.bt-mediacontroller:tsd-bt-media " -f $d -- \
            -U "Partition=rootfs_ro"
         rm -rf $d

   unittests:
      environment:
         TSD_BUILDENV_ENABLE_TEST_TARGETS: '1'
      buildScript: buildenvTestAll
      packageScript: buildenvPackageTests

   doc:
      buildTools: [doxygen, dot]
      buildScript: buildenvBuildDocAll
      packageScript: buildenvPackageDoc
