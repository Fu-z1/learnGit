inherit: [cmake, packing-module]

metaEnvironment:
   PKG_LICENSE: "proprietary"
   PKG_VERSION: "6.00.B030"
   PKG_NAME: "Online Remote Update"
   PKG_PATCH_VERSION: "6.00.B030"
   PKG_RESPONSIBLE: "System-Vernetzung"
   PKG_ARTEFACT_TYPE: "Library"

privateEnvironment:
   CXXFLAGS: "${CXXFLAGS} -fvisibility=hidden"

depends:
  - libs::ext-openssl-dev
  - system::ext-poco-dev
  - system::tsd-common-dev

  - use: []
    depends:
      - libs::ext-openssl-target
      - system::ext-poco-target
      - system::tsd-common-target

checkoutSCM:
  - scm: git
    url: git@${DEFAULT_PCC_GIT_SERVER}:mib3-vehicleapplications/ext.vw.rsi.onlineremoteupdate.git
    dir: ext.vw.rsi.onlineremoteupdate
    tag: SOP1_v_EB_331
    branch: master_mqb_sop1_eu

buildScript: |
   rsync --exclude=".git*" -a --delete $1/ $PWD/
   CMAKE_SRC_PATH="$PWD/ext.vw.rsi.onlineremoteupdate"
   cmakeConfigure -DSHARED_LIBRARY_VERSION=00
   makeParallel
   cmakeInstall install

multiPackage:
   dev:
      packageTools: [buildenv]
      packageScript: |
         cmakePackageDev

         # for debug there is a postfix
         DbgPostFix=""
         if [ "${CMAKE_BUILD_TYPE:-}" == "Debug" ]; then
            DbgPostFix="d"
         fi

         rsync -a --delete $1/dist/ .
         if [ "${LIBDIR:-}" != "lib" ]; then
            mv ./usr/lib ./usr/${LIBDIR}
         fi
         tsd-buildenv-create-shim.sh -p usr -I include -L ${LIBDIR}        \
                                        -l MIBOnlineRemoteUpdateClientLib${DbgPostFix}   \
                                        ext.vw.rsi.onlineremoteupdate
      provideDeps: ["*-dev"]

   target:
      packageScript: |
         cmakePackageTarget
         # the following libs are already build with ServiceRegistry, dont't distribute again
         find ./usr/${LIBDIR}/ -name "*MIBSecurityLib*" -delete
         find ./usr/${LIBDIR}/ -name "*MIBServiceLib*"  -delete
         find ./usr/${LIBDIR}/ -name "*MIBUtilLib*"     -delete
         find ./usr/${LIBDIR}/ -name "*MIBBapLib*"      -delete
         find ./usr/${LIBDIR}/ -name "*MIBMappingServiceLib*" -delete
         find ./usr/${LIBDIR}/ -name "*MIBServiceDataLib*"    -delete
         find ./usr/${LIBDIR}/ -name "*MIBServiceDBLib*"      -delete
         find ./usr/${LIBDIR}/ -name "*MIBClientLib*"         -delete
         d=$(mktemp -d)
         rsync -a --delete --exclude ".debug" ./ $d
         buildSwupModule -n "ext-vw-rsi-oru" \
            -p "emmc.basic-svcs.cfm" \
            -o "basic-services.car-function-management:ext-vw-rsi-onlineremoteupdate " -f $d -- \
            -U "Partition=rootfs_ro"
         rm -rf $d
      provideDeps: ["*-target"]
