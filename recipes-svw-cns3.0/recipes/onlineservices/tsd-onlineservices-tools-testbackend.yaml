inherit: [buildenv, packing-module]

metaEnvironment:
   PKG_RESPONSIBLE: Connect-Onlineservices

depends:
  - system::tsd-common-dev

checkoutSCM:
  - scm: git
    url: git@${DEFAULT_PCC_GIT_SERVER}:mib3-onlineservices/tsd.onlineservices.tools.testbackend.git
    dir: tsd.onlineservices.tools.testbackend
    branch: master_mqb_sop1_eu

multiPackage:
   dev:
      packageScript: buildenvPackageDev
      provideDeps:
        - system::tsd-common-dev

   target:
      buildScript: buildenvBuildAll
      packageScript: |
         buildenvPackageTarget
         d=$(mktemp -d)
         rsync -a --delete --exclude ".debug" ./ $d
         buildSwupModule -n "onlineservices-tools-testback" \
            -p "emmc.onlineservices" \
            -o "onlineservices:tsd-onlineservices-tools-testbackend " -f $d -- \
            -U "Partition=rootfs_ro"
         rm -rf $d

   unittests:
      environment:
         TSD_BUILDENV_ENABLE_TEST_TARGETS: "1"
      buildScript: buildenvTestAll
      packageScript: buildenvPackageTests
