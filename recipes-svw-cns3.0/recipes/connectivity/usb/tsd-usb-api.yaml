inherit: [buildenv, packing-module, capigen, werror]

metaEnvironment:
   PKG_RESPONSIBLE: "Connect-Connection"

depends:
   # build dependencies
 - system::tsd-common-dev
 - communication::ext-commonapi-runtime-dev
 - communication::tsd-communication-binding-dev

   # runtime dependencies
 - use: []
   depends:
    - system::tsd-common-target
    - communication::ext-commonapi-runtime-target
    - communication::tsd-communication-binding-target

checkoutSCM:
 - scm: git
   url: git@${DEFAULT_PCC_GIT_SERVER}:mib3-ceconnect/tsd.usb.api.git
   dir: tsd.usb.api
   tag: mqb_sop1_eu-1.106.0

checkoutScript: CapiGenProcessFIDLFiles tsd.usb.api
checkoutDeterministic: true

multiPackage:
   ? ""
   :  buildScript: buildenvBuildAll
      multiPackage:
         dev:
            packageScript: buildenvPackageDev
            provideDeps: ["*-dev"]

         target:
            packageScript: |
               buildenvPackageTarget
               d=$(mktemp -d)
               rsync -a --delete --exclude ".debug" ./ $d
               buildSwupModule -n "usb-api" \
                  -p "emmc.connectivity.usb" \
                  -o "connectivity.usb:tsd-usb-api " -f $d -- \
                  -U "Partition=rootfs_ro"
               rm -rf $d
            provideDeps: ["*-target"]

   doc:
      buildTools: [doxygen, dot]
      buildScript: buildenvBuildDocAll
      packageScript: buildenvPackageDoc
