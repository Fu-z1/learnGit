inherit: [buildenv, packing-module, werror]

metaEnvironment:
   PKG_RESPONSIBLE: "Connect-Connection"

depends:
   # build dependencies
 - connectivity::usb::tsd-usb-common-dev
 - connectivity::usb::ext-libusb-dev
 - connectivity::usb::ext-libusbgx-dev


   # runtime dependencies
 - use: []
   depends:
    - connectivity::usb::tsd-usb-common-target
    - connectivity::usb::ext-libusb-target
    - connectivity::usb::ext-libusbgx-target

checkoutSCM:
 - scm: git
   url: git@${DEFAULT_PCC_GIT_SERVER}:tip-ceconnect/tsd.usb.osal.linux.git
   dir: tsd.usb.osal.linux
   branch: main/svw_cns3.0_1520_dev
multiPackage:
   ? ""
   :  buildScript: buildenvBuildAll
      multiPackage:
         dev:
            packageScript: buildenvPackageDev
            provideDeps: ["*-dev"]

         target:
            packageScript: |
               buildenvPackageTarget
               d=$(mktemp -d)
               rsync -a --delete --exclude ".debug" ./ $d
               buildSwupModule -n "usb-osal-linux" \
                  -p "emmc.connectivity.usb" \
                  -o "connectivity.usb:tsd-usb-osal-linux " -f $d -- \
                  -U "Partition=rootfs_ro"
               rm -rf $d
            provideDeps: ["*-target"]

   doc:
      buildTools: [doxygen, dot]
      buildScript: buildenvBuildDocAll
      packageScript: buildenvPackageDoc

   unittests:
      environment:
         TSD_BUILDENV_ENABLE_TEST_TARGETS: "1"
      buildScript: buildenvTestTargets tsd.usb.osal.linux.test
      packageScript: buildenvPackageTests

